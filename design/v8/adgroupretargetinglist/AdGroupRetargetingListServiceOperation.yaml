AdGroupRetargetingListServiceOperation:
  type: object
  nullable: true
  required:
  - accountId
  - operand
  properties:
    accountId:
      type: integer
      nullable: false
      format: int64
      description: |
        <div lang="ja">アカウントIDです。</div>
        <div lang="en">Account ID.</div>
    operand:
      type: array
      nullable: true
      items:
        $ref: 'AdGroupRetargetingList.yaml#/AdGroupRetargetingList'
      minItems: 1
      maxItems: 1000
  title: AdGroupRetargetingListServiceOperation
  description: |
    <div lang="ja">AdGroupRetargetingListServiceOperationは、操作対象の広告グループレベルでのターゲットリスト設定を保持するオブジェクトです。</div>
    <div lang="en">AdGroupRetargetingListServiceOperation is an object that holds target ad group retargeting.</div>
