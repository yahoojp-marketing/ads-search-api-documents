CustomizerAttributeServiceSelector:
  type: object
  nullable: true
  required:
  - accountId
  properties:
    accountId:
      type: integer
      nullable: false
      format: int64
      description: |
        <div lang="ja">検索条件：アカウントIDです。</div>
        <div lang="en">Search condition: Account ID.</div>
    types:
      type: array
      nullable: true
      items:
        $ref: 'CustomizerAttributeServiceType.yaml#/CustomizerAttributeServiceType'
      maxItems: 4
  title: CustomizerAttributeServiceSelector
  description: |
    <div lang="ja">CustomizerAttributeServiceSelectorオブジェクトは、カスタマイザー属性の情報を指定します。</div>
    <div lang="en">CustomizerAttributeServiceSelector describes the CustomizerAttribute information.</div>
