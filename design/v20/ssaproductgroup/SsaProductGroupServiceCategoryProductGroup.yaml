SsaProductGroupServiceCategoryProductGroup:
  type: object
  nullable: true
  properties:
    'type':
      $ref: 'SsaProductGroupServiceCategoryType.yaml#/SsaProductGroupServiceCategoryType'
    value:
      type: string
      nullable: true
      description: |
        <div lang="ja">属性値<br>
        このフィールドはカテゴリ名を表し、null の場合は「その他」を表します。<br>
        このフィールドは、ADD時は必須となります。</div>
        <div lang="en">Attribute value.<br>
        This field represents the category name. A null value indicates "Other".<br>
        This field is required in ADD operation.</div>
  title: SsaProductGroupServiceCategoryProductGroup
  description: |
    <div lang="ja">SsaProductGroupServiceCategoryProductGroup オブジェクトは、カテゴリ商品グループを表します。<br>
    ADD時、SsaProductGroupServiceProductGroupTypeがCATEGORYの場合、必須となり、それ以外では無視されます。</div>
    <div lang="en">SsaProductGroupServiceCategoryProductGroup object describes caregroy product group information.<br>
    This field is required when SsaProductGroupServiceProductGroupType is 'CATEGORY' in ADD operation. For other BiddingStrategyType, this field is ignored in ADD operation.</div>
