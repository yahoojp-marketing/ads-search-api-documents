SsaProductGroupServiceProductIdProductGroup:
  type: object
  nullable: true
  properties:
    sku:
      type: string
      nullable: true
      description: |
        <div lang="ja">SKU<br>
        このフィールドは、ADD時は必須となります。</div>
        <div lang="en">SKU<br>
        This field is required in ADD operation.</div>
    value:
      type: string
      nullable: true
      description: |
        <div lang="ja">属性値<br>
        このフィールドは商品IDを表し、null の場合は「その他」を表します。<br>
        このフィールドは、ADD時は必須となります。</div>
        <div lang="en">Attribute value.<br>
        This field represents the product ID. A null value indicates "Other".<br>
        This field is required in ADD operation.</div>
  title: SsaProductGroupServiceProductIdProductGroup
  description: |
    <div lang="ja">SsaProductGroupServiceProductIdProductGroup オブジェクトは、商品ID商品グループを表します。<br>
    ADD時、SsaProductGroupServiceProductGroupTypeがPRODUCT_IDの場合、必須となり、それ以外では無視されます。</div>
    <div lang="en">SsaProductGroupServiceProductIdProductGroup object describes product id product group information.<br>
    This field is required when SsaProductGroupServiceProductGroupType is 'PRODUCT_ID' in ADD operation. For other BiddingStrategyType, this field is ignored in ADD operation.</div>
