SsaProductGroupServiceValue:
  type: object
  nullable: true
  properties:
    productGroup:
      $ref: 'SsaProductGroup.yaml#/SsaProductGroup'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果を表示します。</div>
        <div lang="en">Result of process.</div>
  title: SsaProductGroupServiceValue
  description: |
    <div lang="ja">SsaProductGroupServiceValueオブジェクトは、処理結果を含む商品グループに関する情報を表します。</div>
    <div lang="en">SsaProductGroupServiceValue object displays product group information including its operation results.</div>
