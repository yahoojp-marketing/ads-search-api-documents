SsaProductGroupServiceSellerIdProductGroup:
  type: object
  nullable: true
  properties:
    value:
      type: string
      nullable: true
      description: |
        <div lang="ja">属性値<br>
        このフィールドはセラーIDを表し、null の場合は「その他」を表します。<br>
        このフィールドは、ADD時は必須となります。</div>
        <div lang="en">Attribute value.<br>
        This field represents the seller ID. A null value indicates "Other".<br>
        This field is required in ADD operation.</div>
  title: SsaProductGroupServiceSellerIdProductGroup
  description: |
    <div lang="ja">SsaProductGroupServiceSellerIdProductGroup オブジェクトは、セラーID商品グループを表します。<br>
    ADD時、SsaProductGroupServiceProductGroupTypeがSELLER_IDの場合、必須となり、それ以外では無視されます。</div>
    <div lang="en">SsaProductGroupServiceSellerIdProductGroup object describes seller id product group information.<br>
    This field is required when SsaProductGroupServiceProductGroupType is 'SELLER_ID' in ADD operation. For other BiddingStrategyType, this field is ignored in ADD operation.</div>
