AccountAssetServiceValue:
  type: object
  nullable: true
  properties:
    accountAsset:
      $ref: 'AccountAsset.yaml#/AccountAsset'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果を表示します。</div>
        <div lang="en">Showing operation results.</div>
  title: AccountAssetServiceValue
  description: |
    <div lang="ja">AccountAssetServiceValueオブジェクトは、アカウントとアセット間の設定情報を格納するコンテナです。</div>
    <div lang="en">AccountAssetServiceValue is a container of configuration information between accounts and assets.</div>
