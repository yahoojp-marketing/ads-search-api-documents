SsaAdGroupServiceValue:
  type: object
  nullable: true
  properties:
    adGroup:
      $ref: 'SsaAdGroup.yaml#/SsaAdGroup'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果を表示します。</div>
        <div lang="en">Result of process.</div>
  title: SsaAdGroupServiceValue
  description: |
    <div lang="ja">SsaAdGroupServiceValueオブジェクトは、処理結果を含む広告グループに関する情報を表します。</div>
    <div lang="en">SsaAdGroupServiceValue object displays ad group information including its operation results.</div>
