KeywordPlanIdeaServiceKeywordAndUrlSeed:
  type: object
  nullable: true
  properties:
    keywords:
      type: array
      nullable: true
      items:
        type: string
        nullable: true
      description: |
        <div lang="ja">キーワードです。</div>
        <div lang="en">Keyword.</div>
    url:
      type: string
      nullable: true
      description: |
        <div lang="ja">URLです。</div>
        <div lang="en">URL.</div>
  title: KeywordPlanIdeaServiceKeywordAndUrlSeed
  description: |
    <div lang="ja">KeywordPlanIdeaServiceKeywordAndUrlSeedオブジェクトは、キーワード候補作成条件としてキーワードとURLを指定するオブジェクトです。<br>seedTypeが<code>KEYWORD_AND_URL</code>の場合は必須です。</div>
    <div lang="en">KeywordPlanIdeaServiceKeywordAndUrlSeed object is for specifying keywords and URL as related keywords creation conditions.<br>If seedType is <code>KEYWORD_AND_URL</code>, this field is required.</div>
