AudienceListServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number the item have retrieved.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'AudienceListServiceValue.yaml#/AudienceListServiceValue'
  title: AudienceListServicePage
  description: |
    <div lang="ja">AudienceListServicePageは、オーディエンスリストの実行結果（全Entityのリスト）を保持するオブジェクトです。</div>
    <div lang="en">AudienceListServicePage is an object that holds results (list of all entity).</div>
