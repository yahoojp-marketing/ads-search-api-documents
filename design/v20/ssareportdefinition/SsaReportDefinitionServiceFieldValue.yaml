SsaReportDefinitionServiceFieldValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    fields:
      type: array
      nullable: true
      items:
        $ref: 'SsaReportDefinitionServiceReportFieldAttribute.yaml#/SsaReportDefinitionServiceReportFieldAttribute'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: SsaReportDefinitionServiceFieldValue
  description: |
    <div lang="ja">SsaReportDefinitionServiceFieldValueオブジェクトは、取得される使用可能なレポートのエントリーを表します。</div>
    <div lang="en">SsaReportDefinitionServiceFieldValue object displays an entry of available reports by get operations.</div>
