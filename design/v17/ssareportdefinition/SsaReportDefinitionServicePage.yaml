SsaReportDefinitionServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of entries in the result that this page is a part of.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'SsaReportDefinitionServiceValue.yaml#/SsaReportDefinitionServiceValue'
  title: SsaReportDefinitionServicePage
  description: |
    <div lang="ja">SsaReportDefinitionServicePageオブジェクトは、取得されるレポートのエントリーを表します。</div>
    <div lang="en">Entry obtained from get operations.</div>
