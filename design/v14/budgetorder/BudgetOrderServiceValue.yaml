BudgetOrderServiceValue:
  type: object
  nullable: true
  properties:
    budgetOrder:
      $ref: 'BudgetOrder.yaml#/BudgetOrder'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: BudgetOrderServiceValue
  description: |
    <div lang="ja">BudgetOrderServiceValueオブジェクトは、アカウント予算情報を格納するコンテナです。</div>
    <div lang="en">The BudgetOrderServiceValue object is a container for storing account budget.</div>
