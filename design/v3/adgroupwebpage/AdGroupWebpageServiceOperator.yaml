AdGroupWebpageServiceOperator:
  type: string
  nullable: true
  enum:
  - CONTAINS
  - EQUALS
  - UNKNOWN
  title: AdGroupWebpageServiceOperator
  description: |
    <div lang="ja">AdGroupWebpageServiceOperatorは、URLの条件種別です。<br>
    このフィールドは、webpageConditionTypeがURLの場合のみ指定できます。<br>
    operatorにEQUALSを指定する場合は、conditionsに1件のみ指定できます。<br>
    また、このフィールドはSETおよびREMOVE時に無視されます。</div>
    <div lang="en">AdGroupWebpageServiceOperator is a condition type of URL.<br>
    This field can be specified only if the webpageConditionType is URL. <br> 
    If you specified EQUALS in operator, you can specify only one value in condition. <br>
    This field will be ignored in SET and REMOVE operation. </div>
    <hr>
    <p>* <code>CONTAINS</code> - <span lang="ja">指定の値を含みます。</span><span lang="en">Contains the selected value.</span></p>
    <p>* <code>EQUALS</code> - <span lang="ja">指定の値と一致します。</span><span lang="en">Equal to selected value.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>
