KeywordForecastMetricsServiceValue:
  type: object
  nullable: true
  properties:
    keywordForecastMetrics:
      $ref: 'KeywordForecastMetrics.yaml#/KeywordForecastMetrics'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。</div>
        <div lang="en">Operation result.</div>
  title: KeywordForecastMetricsServiceValue
  description: |
    <div lang="ja">KeywordForecastMetrics オブジェクトは、見積もりリクエストを格納します。</div>
    <div lang="en">KeywordForecastMetrics object contains estimate request.</div>
