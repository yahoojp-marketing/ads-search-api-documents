PageFeedAssetSetServiceValue:
  type: object
  nullable: true
  properties:
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    pageFeedAssetSet:
      $ref: 'PageFeedAssetSet.yaml#/PageFeedAssetSet'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: PageFeedAssetSetServiceValue
  description: |
    <div lang="ja">PageFeedAssetSetServiceValueオブジェクトは、ページフィードアセットセットを格納するコンテナです。</div>
    <div lang="en">PageFeedAssetSetServiceValue object is a container that stores a page feed asset set.</div>
