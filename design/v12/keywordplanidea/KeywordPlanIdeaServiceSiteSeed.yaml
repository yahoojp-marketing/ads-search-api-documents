KeywordPlanIdeaServiceSiteSeed:
  type: object
  nullable: true
  properties:
    site:
      type: string
      nullable: true
      description: |
        <div lang="ja">サイト（ドメインURL）です。</div>
        <div lang="en">Site (domain URL).</div>
  title: KeywordPlanIdeaServiceSiteSeed
  description: |
    <div lang="ja">KeywordPlanIdeaServiceSiteSeedオブジェクトは、キーワード候補作成条件としてサイト（ドメインURL）を指定するオブジェクトです。<br>seedTypeが<code>SITE</code>の場合は必須です。</div>
    <div lang="en">KeywordPlanIdeaServiceSiteSeed object is for specifying site (domain URL) as related keywords creation conditions.<br>If seedType is <code>SITE</code>, this field is required.</div>
