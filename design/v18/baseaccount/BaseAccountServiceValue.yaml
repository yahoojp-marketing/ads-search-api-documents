BaseAccountServiceValue:
  type: object
  nullable: true
  properties:
    account:
      $ref: 'BaseAccount.yaml#/BaseAccount'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: BaseAccountServiceValue
  description: |
    <div lang="ja">BaseAccountServiceValueオブジェクトは、アカウント情報を格納するコンテナです。</div>
    <div lang="en">The BaseAccountServiceValue object is a container for storing account information.</div>
