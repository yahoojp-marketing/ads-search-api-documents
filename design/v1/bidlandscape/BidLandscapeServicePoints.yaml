BidLandscapeServicePoints:
  type: object
  nullable: true
  properties:
    bid:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">この状況点のデータを予測するために使用される入札単価です。</div>
        <div lang="en">The bid unit price used in order to estimate the data of this situation point.</div>
    clicks:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">この入札単価での予測クリック数です。</div>
        <div lang="en">The number of estimate clicks in this bid unit price.</div>
    cost:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">この入札単価での予測コストです。</div>
        <div lang="en">The estimate cost in this bid unit price.</div>
    impressions:
      type: integer
      nullable: true
      format: int64
      description: |
        <div lang="ja">この入札単価での予測インプレッションです。</div>
        <div lang="en">The estimate impressions in this bid unit price.</div>
  title: BidLandscapeServicePoints
  description: |
    <div lang="ja">LandscapePointsオブジェクトは、特定の入札単価に対する一連の予測結果を表します。</div>
    <div lang="en">Series of expected results of a specific bid.</div>
