AdGroupAdServiceValue:
  type: object
  nullable: true
  properties:
    adGroupAd:
      $ref: 'AdGroupAd.yaml#/AdGroupAd'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: "<div lang=\"ja\">処理結果です。<br>trueの場合は、処理は成功しました。<br>falseの場合は処理が失敗しています。</div><div lang=\"en\">The process results.<br>If true, the process succeeded.<br>If false, the process failed.</div>"
  title: AdGroupAdServiceValue
  description: "<div lang=\"ja\">AdGroupAdServiceValueオブジェクトは、処理結果を含む広告に関する情報を表します。</div><div lang=\"en\">Ad\
    \ including operation results.</div>"
