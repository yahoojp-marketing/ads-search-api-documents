AdGroupLabelServiceValue:
  type: object
  nullable: true
  properties:
    adGroupLabel:
      $ref: 'AdGroupLabel.yaml#/AdGroupLabel'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div>
        <div lang="en">The process results. If true, the process succeeded. If false, the process failed.</div>
  title: AdGroupLabelServiceValue
  description: |
    <div lang="ja">AdGroupLabelServiceValueオブジェクトは、操作結果を含む広告グループラベルの情報を表します。</div>
    <div lang="en">AdGroupLabelServiceValue object describes ad group label information including its operation results.</div>
