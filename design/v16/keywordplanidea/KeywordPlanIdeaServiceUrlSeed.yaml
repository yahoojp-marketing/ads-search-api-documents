KeywordPlanIdeaServiceUrlSeed:
  type: object
  nullable: true
  properties:
    url:
      type: string
      nullable: true
      description: |
        <div lang="ja">URLです。</div>
        <div lang="en">URL.</div>
  title: KeywordPlanIdeaServiceUrlSeed
  description: |
    <div lang="ja">KeywordPlanIdeaServiceUrlSeedオブジェクトは、キーワード候補作成条件としてURLを指定するオブジェクトです。<br>seedTypeが<code>URL</code>の場合は必須です。</div>
    <div lang="en">KeywordPlanIdeaServiceUrlSeed object is for specifying URL as related keywords creation conditions.<br>If seedType is <code>URL</code>, this field is required.</div>
