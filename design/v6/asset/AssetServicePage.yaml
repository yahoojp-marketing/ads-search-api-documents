AssetServicePage:
  type: object
  nullable: true
  properties:
    totalNumEntries:
      type: integer
      nullable: false
      format: int32
      description: |
        <div lang="ja">取得される項目の総件数です。</div>
        <div lang="en">Total number of entries in the result.</div>
    values:
      type: array
      nullable: true
      items:
        $ref: 'AssetServiceValue.yaml#/AssetServiceValue'
  title: AssetServicePage
  description: |
    <div lang="ja">AssetServicePageオブジェクトは、アセットの情報を格納します。</div>
    <div lang="en">AssetServicePage contains the Asset information.</div>
