CampaignTargetServiceLocationTarget:
  type: object
  nullable: true
  properties:
    cityNameEN:
      type: string
      nullable: true
      description: |
        <div lang="ja">市区町村名（英語）です。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
        <div lang="en">City(English).<br> Although this field will be returned in the response, it will be ignored on input.</div>
    cityNameJA:
      type: string
      nullable: true
      description: |
        <div lang="ja">市区町村名（日本語）です。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
        <div lang="en">City(Japanese).<br> Although this field will be returned in the response, it will be ignored on input.</div>
    excludedType:
      $ref: 'CampaignTargetServiceExcludedType.yaml#/CampaignTargetServiceExcludedType'
    provinceNameEN:
      type: string
      nullable: true
      description: |
        <div lang="ja">都道府県名（英語）です。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
        <div lang="en">Prefecture(English).<br> Although this field will be returned in the response, it will be ignored on input.</div>
    provinceNameJA:
      type: string
      nullable: true
      description: |
        <div lang="ja">都道府県名（日本語）です。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
        <div lang="en">Prefecture(Japanese).<br> Although this field will be returned in the response, it will be ignored on input.</div>
    targetingStatus:
      $ref: 'CampaignTargetServiceTargetingStatus.yaml#/CampaignTargetServiceTargetingStatus'
  title: CampaignTargetServiceLocationTarget
  description: |
    <div lang="ja">CampaignTargetServiceLocationTargetオブジェクトは、地域ターゲティング設定です。<br>
    このフィールドは、ADDおよびSET時に省略可能となります。<br>
    ※targetTypeがLOCATIONの場合、このフィールドはADD時に必須となります。</div>
    <div lang="en">CampaignTargetServiceLocationTarget object is a location target setting.<br>
    This field is optional in ADD and SET operation.<br>
    *If targetType is LOCATION, this field is required in ADD operation.</div>
