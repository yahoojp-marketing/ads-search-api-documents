AdGroupFeedServiceSelector:
  type: object
  required:
  - accountId
  properties:
    accountId:
      type: integer
      format: int64
      description: "<ja>アカウントIDです。</ja><br><en>Account ID.</en>"
    adGroupIds:
      type: array
      items:
        type: integer
        format: int64
      maxItems: 500
      description: "<ja>広告グループIDです。</ja><br><en>Ad group ID.</en>"
    campaignIds:
      type: array
      items:
        type: integer
        format: int64
      maxItems: 500
      description: "<ja>キャンペーンIDです。</ja><br><en>Campaign ID.</en>"
    feedItemIds:
      type: array
      items:
        type: integer
        format: int64
      maxItems: 500
      description: "<ja>FeedItem情報のIDです。</ja><br><en>FeedItem information ID.</en>"
    numberResults:
      type: integer
      format: int32
      minimum: 1
      maximum: 500
      default: 500
      description: "ページの最大件数です。このフィールドは、1以上を指定する必要があります。<br>Maximum number of results\
        \ to return in this page. This field must be greater than or equal to 1. Also\
        \ see Entity Limits per operation."
    placeholderTypes:
      type: array
      items:
        $ref: 'AdGroupFeedServicePlaceholderType.yaml#/AdGroupFeedServicePlaceholderType'
      maxItems: 4
    startIndex:
      type: integer
      format: int32
      minimum: 1
      default: 1
      description: "ページの先頭のインデックスです。このフィールドは、1以上を指定する必要があります。<br>Index of the first\
        \ result to return in this page. This field must be greater than or equal\
        \ to 1."
  title: AdGroupFeedServiceSelector
  description: "<ja>AdGroupFeedServiceSelectorオブジェクトは、FeedItem情報を指定します。</ja><br><en>AdGroupFeedServiceSelector\
    \ describes the FeedItem information.</en>"
