AuditLogServicePage:
  type: object
  properties:
    totalNumEntries:
      type: integer
      format: int32
      description: "<ja>取得される項目の総件数です。</ja><br><en>Total number of entries in the result that this page is a part of.</en>"
    values:
      type: array
      items:
        $ref: 'AuditLogServiceValue.yaml#/AuditLogServiceValue'
  title: AuditLogServicePage
  description: "<ja>AuditLogServicePageオブジェクトは、実行結果(全エンティティのリスト)を保持します。</ja><br><en>AuditLogServicePage\
    \ object retains the execution result (list of all entities).</en>"
