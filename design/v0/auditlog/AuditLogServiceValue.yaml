AuditLogServiceValue:
  type: object
  properties:
    errors:
      type: array
      items:
        $ref: '../../Error.yaml#/Error'
    job:
      $ref: 'AuditLogServiceJob.yaml#/AuditLogServiceJob'
    operationSucceeded:
      type: boolean
      description: "<ja>処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</ja><br><en>The
                    process results. If true, the process succeeded. If false, the process failed.</en>"
  title: AuditLogServiceValue
  description: "<ja>AuditLogServiceValueオブジェクトは、実行結果（1エンティティ）を保持します。</ja><br><en>AuditLogServiceValue\
    \ object retains the operation result (1 entity).</en>"
