CampaignExportServiceFieldValue:
  type: object
  properties:
    errors:
      type: array
      items:
        $ref: '../../Error.yaml#/Error'
    fieldAttributes:
      type: array
      items:
        $ref: 'CampaignExportServiceFieldAttribute.yaml#/CampaignExportServiceFieldAttribute'
    operationSucceeded:
      type: boolean
      description: "<div lang=\"ja\">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div><div lang=\"en\">The
                    process results. If true, the process succeeded. If false, the process failed.</div>"
  title: CampaignExportServiceFieldValue
  description: "<div lang=\"ja\">CampaignExportServiceFieldValue オブジェクトは、実行結果(全Entityのリスト)を保持します。</div><div lang=\"en\">CampaignExportServiceFieldValue\
    \ object contains the results (list of all entities).</div>"
