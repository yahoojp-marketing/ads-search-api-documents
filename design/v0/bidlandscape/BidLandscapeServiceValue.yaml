BidLandscapeServiceValue:
  type: object
  nullable: true
  properties:
    data:
      $ref: 'BidLandscape.yaml#/BidLandscape'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: |
        <div lang="ja">処理結果です。</div>
        <div lang="en">The result of operation.</div>
  title: BidLandscapeServiceValue
  description: |
    <div lang="ja">BidLandscapeServiceValueオブジェクトは、予測処理の結果を格納するコンテナです。</div>
    <div lang="en">Container for estimated operation results.</div>
