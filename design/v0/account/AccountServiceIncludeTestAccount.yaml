AccountServiceIncludeTestAccount:
  type: string
  default: EXCLUDE_TEST
  enum:
    - ONLY_TEST
    - EXCLUDE_TEST
    - ALL
    - UNKNOWN
  title: AccountServiceIncludeTestAccount
  description: |
    <div lang="ja">取得アカウント制御</div>
    <div lang="en">Control which accounts are retrieved.</div><hr>
    * <code>ONLY_TEST</code> - <div lang="ja">テストアカウントのみ</div><div lang="en">Test account only.</div>
    * <code>EXCLUDE_TEST</code> - <div lang="ja">テストアカウントを除外</div><div lang="en">Exclude test accounts.</div>
    * <code>ALL</code> - <div lang="ja">全てのアカウント</div><div lang="en">All accounts.</div>
    * <code>UNKNOWN</code> - <div lang="ja">未知の値です。</div><div lang="en">Unknown Value</div>
