AccountServiceValue:
  type: object
  properties:
    account:
      $ref: 'Account.yaml#/Account'
    errors:
      type: array
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      description: "<ja>処理結果です。</ja><br><en>The result of operation.</en>"
  title: AccountServiceValue
  description: "<ja>AccountServiceValueオブジェクトは、操作結果を含むアカウント情報を表します。</ja><br><en>Account\
    \ information including operation results.</en>"
