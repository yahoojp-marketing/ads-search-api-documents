AdGroupWebpageServiceValue:
  type: object
  nullable: true
  properties:
    adGroupWebpage:
      $ref: 'AdGroupWebpage.yaml#/AdGroupWebpage'
    errors:
      type: array
      nullable: true
      items:
        $ref: '../../Error.yaml#/Error'
    operationSucceeded:
      type: boolean
      nullable: true
      description: "<div lang=\"ja\">処理結果です。trueの場合は、処理は成功しました。falseの場合は処理が失敗しています。</div><div lang=\"en\">The
        process results. If true, the process succeeded. If false, the process failed.</div>"
  title: AdGroupWebpageServiceValue
  description: "<div lang=\"ja\">AdGroupWebpageServiceValueオブジェクトは、操作結果を含むPageFeedItemの配信/除外設定を表します。</div><div lang=\"en\">AdGroupWebpageServiceValue\
    \ object describes allowed or excluded settings including operation result.</div>"
